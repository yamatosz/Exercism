from lists import (card_average,
                   maybe_double_last,
                   concatenate_rounds,
                   list_contains_round,
                   average_even_is_average_odd,
                   approx_average_is_average)

print(average_even_is_average_odd([1, 2, 3]))
hand = [5, 9, 10]
print(maybe_double_last(hand))